if hl.plugin and hl.plugin.scrolloverview then
    hl.config({
        plugin = {
            scrolloverview = {
                scale = 0.5,
                workspace_gap = 20,
                layout = "vertical",
                wallpaper = 0,
                blur = false,

                shadow = {
                    enabled = false,
                },
            },
        },
    })
end